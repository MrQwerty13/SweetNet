# Copy to .env.native, chmod 600, and fill DATABASE_URL using your own dedicated database.
# This file is shell-compatible. Quote values; do not commit .env.native.
DATABASE_URL=''
APP_ENV=development
APP_ORIGIN=http://127.0.0.1:8080
HTTP_ADDR=127.0.0.1:8080
UPLOAD_DIR=.local/uploads
WEB_DIR=web/dist
